# Concept: {{ project_name }}

> **Это immutable документ** — фиксирует начальное видение проекта
> на момент его создания. После заполнения **не редактируется**.
> Текущее состояние проекта ведётся в `README.md`; если концепция
> кардинально меняется (rare случай, pivot) — создаётся новая
> версия в `concepts/v1-...md`, `concepts/v2-...md` (ADR-pattern,
> но для концепций).
>
> **Дата создания:** `<YYYY-MM-DD>`
>
> При создании проекта через `dreamteam init` этот файл уже на месте;
> пройди ритуал встречных вопросов с Claude (см. ниже), заполни
> разделы и зафиксируй как immutable.

<!-- Структура ниже — ОПРОСНИК (leading questions) для пустого
     concept, а не обязательная форма. Если у тебя уже есть
     содержательный concept / ТЗ / vision — замени содержимое этого
     файла целиком своим, ничего не перекладывая в эти заголовки.
     Главное, чтобы в любой форме присутствовал раздел «Out of scope»
     (защита от scope creep), и чтобы документ остался immutable
     после фиксации. Ритуал clarify (встречные вопросы Claude по
     слепым зонам) проводится по фактическому содержимому. -->

## Цель

<!-- 1-2 предложения. Что делает проект и зачем нужен. Без
     технических деталей и обещаний реализации — только смысл. -->

## Пользователь

<!-- Для кого. Use case на одной-двух фразах. Если пользователь —
     сам Разработчик, так и пишем; если это будущая «третья
     сторона» — описать кратко её контекст. -->

## Ключевая функциональность

<!-- 3-7 пунктов на уровне «возможностей», не реализации. Каждый
     пункт отвечает на вопрос «что система должна уметь?», а не
     «как». -->

- ...

## Out of scope

<!-- Что СОЗНАТЕЛЬНО НЕ делает этот проект. Этот раздел — главная
     защита от расползания scope с самого старта. Пиши прямо: «X
     — не делаем» / «Y — оставляем людям/внешним системам». -->

- ...

## Ограничения и догадки

<!-- Платформа, стек (если уже известен), нефункциональные требования
     (производительность, надёжность, безопасность), ассумпции про
     окружение / пользователей / нагрузку. Догадки тоже сюда — они
     потом могут оказаться неверными, и важно зафиксировать, что
     именно мы предполагали в момент старта. -->

- ...

---

## Ритуал составления (вспомогательный текст, удалить после заполнения)

В начале нового проекта `CONCEPT.md` заполняется через **ритуал
встречных вопросов**, аналогично `clarify` для спеки крупной фичи:

1. Разработчик пишет первый набросок секций (или просто формулирует
   идею).
2. Claude задаёт встречные вопросы по слепым зонам:
   - **Цель:** какую боль / задачу проект решает?
   - **Пользователь:** кто конкретно пользователь, в каком контексте?
   - **Ключевая функциональность:** что — минимальный набор для MVP?
     Что — потенциально-нужное, но не сейчас?
   - **Out of scope:** что точно НЕ делаем? (Особое внимание — это
     самая ценная секция для защиты от scope creep.)
   - **Ограничения:** платформа, стек, целевая нагрузка, ассумпции?
3. Ответы Разработчика вшиваются в соответствующие секции.
4. После заполнения этот вспомогательный раздел («Ритуал
   составления») удаляется. `CONCEPT.md` помечается датой и
   считается immutable.
